from setuptools import setup

setup(
    name="compliant_control",
    version="0.1",
    description="A bundle of python packages used to achieve compliant control on the Clearpath Dingo with Kinova Gen3 lite arm.",
    packages=["compliant_control"],
    zip_safe=False,
)
